<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quill Spot</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=DM+Serif+Text&display=swap" rel="stylesheet">
    <link href="styles.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-lg">
        <div class="container-fluid">
          <a class="navbar-brand" href="/"><img src="assets/Quill Spot LOGO.png" height="70px"></a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item">
                    <a class="nav-link" href="/homepage">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/create">Create a Blog</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/about">About Us</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/contact">Contact</a>
                </li>
                
            </ul>
        </div>
        </div>
      </nav>
      <% const month = ["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"] %>
      <% if(locals.title && locals.author && locals.blog) { %> 
        <div class="created d-flex flex-column align-items-center">
            <h1 class="create-head"><%= title %></h1>
            <h4>Author - <%= author %></h4>
            <h4><% const today = new Date(); let dd = today.getDate(); let mm = month[today.getMonth()]; let yy = today.getFullYear(); %><%= mm+" "+dd+", "+yy %></h4>
            <% if (locals.image) { %>
                <img src="<%= image %>" alt="Uploaded Image" class="img-fluid" style="height: auto;">
            <% } %>
            <p><%= blog %></p>
        </div>
        <% } else { %>
            <div class="container">
                <div class="create-head d-flex flex-column align-items-center  ">
                    <h1>This is a safe space to write your heart out!</h1>
                </div>
                <div class="create-form mt-3">
                    <form action="/submit" method="POST" enctype="multipart/form-data">
                        <label for="name" class="form-label">Your Name</label>
                        <input type="text" class="form-control mb-3" name="name" placeholder="Eg: Steve Jobs" required>
                        <label for="title" class="form-label">Title</label>
                        <input type="text" class="form-control mb-3" name="title" placeholder="Eg: How Apple Was Born" required>
                        <label for="blog-content" class="form-label">Start writing 👇🏽</label>
                        <textarea rows="15" name="blog-content" class="form-control mb-3" placeholder="Once upon a time..."></textarea>
                        <label for="file" class="form-label">Upload an Image (Optional)</label>
                        <div class="input-group">
                            <input type="file" class="form-control" name="file" id="inputGroupFile04" aria-describedby="inputGroupFileAddon04" aria-label="Upload">
                            <div class="centered-btn">
                                <button type="button" class="btn" id="upload-button">Upload</button>
                            </div>
                        </div>
                        <div id="upload-success" style="display: none;">
                            <span style="color: green;">&#10003; Image uploaded successfully</span>
                        </div>
                        <div class="centered-btn">
                            <input type="submit" class="btn" value="Submit">
                        </div>
                    </form>
                    <p class="create-head">(This blog will not be saved)</p>
                </div>
              </div>
        <% } %>
      <%- include("partials/footer.ejs") %>
      <script>
        document.getElementById('upload-button').addEventListener('click', function() {
            const fileInput = document.getElementById('inputGroupFile04');
            if (fileInput.files.length > 0) {
                document.getElementById('upload-success').style.display = 'block';
            } else {
                alert('Please select a file to upload.');
            }
        });
    </script>
      <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
      
</body>
</html>